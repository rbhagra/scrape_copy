Common errors:

Adjustments:

- adjusted to only copy from section 1 onwards of CA bill to avoid pre-bill text.
- created check for duplicates and an option to allow / disallow duplicates (of both HTML storing and txt storing).
  - choice is through boolean flag in main and checks for truth of flag in html_store and txt_store
 - installed general check for dynamically-loaded content, e.g Utah, which doesn't load most mill text in intial html without
 it actually appearing as such
    - filters for tells in javascript and for json, as well as compares agaisnt length benchmarks of html verus text
 - uses selenium to load source fully and sorts through common container ids to give best chance of pulling full text 
- added in error handling for cursor / connection closing, especially when multiple versions of a bill are returned in duplicate check and multiple ids 
are present, which originally prevented cursor from closing.

- To do: 

1) add in handling for PDFs. Quick check. 

2) Add handling for legiscan. 

3) develop some check against platform that only shows one page at a time